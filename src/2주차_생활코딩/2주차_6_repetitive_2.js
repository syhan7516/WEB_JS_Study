document.write("<div>6장 반복문 활용</div>");

// document.querySelectorAll(태그); 
// HTML에 존재하는 해당 태그 모두를 노드 리스트로 반환!
// ex) let alist = document.querySelectorAll('~');

// ---------------------------------------------------------
// 실습 1 : .querySelector('')와 .querySelectorAll('') 차이!
// 프로덕트 실습 7 : 프로덕트 html에서 버튼 클릭 시 <a>태그 내 글자색을 ON, OFF 때에 따라 다르게 바꿔보기

// ---------------------------------------------------------
// 1. 실습 정답
// : Selecotor는 해당 태그 중 가장 먼저 나오는 것 하나를 가져온다!
// : Selecotor는 해당 태그를 다 가져와 노드 리스트로 반환해준다!

