document.write("<div>2장 배열 내장 함수 요소 검색 & 확인</div>");

// // 데이터
// const test = ['네이버','다음','구글','아마존','금오공대'];

// // 배열.includes(값)
// const result1 = test.includes('네이버');
// console.log(result1);

// // 배열.indexOf(값)
// const result2 = test.indexOf('네이버');
// console.log(result2);

// // 배열.findIndex(함수)
// const result3 = test.findIndex((ele)=>{return ele === '다음'});
// console.log(result3);

// // 배열.find(함수)
// const result4 = test.find((ele)=>{return ele === '다음'});
// console.log(result4);

// // 수업 내용

// // includes(값)은 true or false

// // indexOf(값)은 있으면 인덱스를!, 없으면 -1

// // findIndex(함수)는 있으면 인덱스를!, 없으면 -1을 반환
// // 반환된 값 변수로 받기 가능!, 원본 배열 지장 X

// // find(함수)는 있다면 해당 값을!, 없으면 undefined를 반환
// // 반환된 값 변수로 받기 가능!, 원본 배열 지장 X

// // ---------------------------------------------------------
// // 실습 1 : 

// // ---------------------------------------------------------
// //  1. 실습 정답